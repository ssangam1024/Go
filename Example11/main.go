package main

import "fmt"

func main() {
	var i, j string = "Hello", "World" // assigning varable i and j datatype as stiring and values as hello, world
	// in this function print() prits its arguments in same line with no space in between them
	fmt.Print(i) // prints varable i value hello
	fmt.Print(j) // print varable j value world beside hellow in the same line without any space
}
