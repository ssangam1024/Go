package main

import "fmt"

func main() {
	var i, j string = "Hello", "World" // assigning varable i and j datatype as stiring and values as hello, world
	//\n prints argument in new line or next line
	// function print() prits its arguments in same line with no space in between them
	fmt.Print(i, "\n") // this function prints i value \n starts in next line
	fmt.Print(j, "\n") // this function prints j value
}
